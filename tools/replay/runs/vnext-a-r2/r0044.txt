```json
{
  "recap": "promotions: root cause found — images appended instead of replaced in refresh.",
  "proposal": "Verify downstream isn't tied to image order or flag survival, then fix. Go?",
  "topic": "Image refresh append bug identified",
  "goal": "Fix Kopi template rendering wrong product images due to image pool contamination.",
  "happened": "Agent identified that refreshProductImagesInPlan appends instead of replaces, mixed-namespace images result, guard blocks updates.",
  "nextStep": "Verify downstream code doesn't depend on image ordering or selected-flag survival before writing fix.",
  "question": "Verify dependencies before fixing?",
  "risk": "Replacing array is larger blast radius than appending; image ordering and flags must not be downstream assumptions."
}
```
