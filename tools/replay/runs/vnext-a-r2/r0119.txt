```json
{
  "recap":    "promotions: root cause confirmed—Kopi backend recomputes every call, hitting 60s limit.",
  "proposal": "Shift scope to Kopi cache fix (Kopi repo, full pipeline). Start?",
  "topic":    "Kopi backend cache needed—root cause found",
  "goal":     "Resolve dashboard performance degradation (root cause in Kopi backend)",
  "happened": "Kopi backend recomputes full Shopify+Amazon on every call, hitting 60s timeout",
  "nextStep": "Implement cache on Kopi backend (scope shift to Kopi repo, full e2e pipeline) or manually warm production as stopgap",
  "question": "Should we shift scope to Kopi backend, or proceed with production stopgap for now?",
  "risk":     "00:00 UTC cron will likely fail and page again; Kopi fix requires full e2e pipeline (not quick patch)"
}
```
