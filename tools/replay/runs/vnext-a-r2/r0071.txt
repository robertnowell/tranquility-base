```json
{
  "recap": "Promotions: cold-load root cause found — upstream slow, cache invalidation bug.",
  "proposal": "Build per-SKU threshold editor. Is the Limit production lead time? Go?",
  "topic": "Inventory alerts: performance and thresholds",
  "goal": "Fix dashboard cold loads and enable per-SKU alert configuration",
  "happened": "Cold-load root cause diagnosed: Kopi upstream slow (47–60s), cache invalidation flaw",
  "nextStep": "Clarify Limit definition, then build per-SKU threshold editor",
  "question": "Should the editable Limit be production lead time, EXW → Reno?",
  "risk": "Separate cache fix required; editor won't resolve the cold-load issue"
}
```
