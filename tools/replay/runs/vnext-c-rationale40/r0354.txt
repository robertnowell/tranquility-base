```json
{
  "recap": "content-engine: Kopi email generation exceeded 28 minutes, polling stopped.",
  "proposal": "Check the URL directly when ready, resume polling if needed. Proceed?",
  "rationale": "The email queued normally and Kopi reported no error—it's just slow today. We stopped polling at the threshold you set to avoid runaway waits. The email ID and URL are stable; we can pick it up whenever it finishes, or poll again now if you'd rather not wait.",
  "topic": "Kopi email render timeout",
  "goal": "Produce and export on-brand email digest to HTML file",
  "happened": "Email generation stalled at `generating` status past the 28-minute threshold without error",
  "nextStep": "Resume polling or check URL manually when Kopi finishes rendering",
  "question": "Resume polling now, or check back later?",
  "risk": "If you resume immediately, you may hit the same delay again; if you defer, the email ID remains valid, but you'll need to track it separately."
}
```
