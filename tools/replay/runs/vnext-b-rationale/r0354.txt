```json
{
  "recap": "content-engine: Kopi email generation stalled past 28 minutes, still generating.",
  "proposal": "Check the URL directly when ready, resume polling later. Proceed?",
  "rationale": "We propose deferring rather than continuing the poll loop because the agent has already exceeded the 28-minute threshold specified in the brief, and Kopi is not reporting an error — it is simply slow. Continuing to poll would burn cycles on a task that has no deadline stated in the original request. The email exists at a stable URL and will eventually render; resuming the check when you return costs nothing. We need to be careful about assuming Kopi is stuck; slow render times on complex assets are normal, and the URL remains accessible for manual inspection.",
  "topic": "Kopi email render timeout",
  "goal": "Export a finished on-brand email from brief file to HTML",
  "happened": "Email generation queued successfully but did not reach success status within 28 minutes; poll loop stopped at threshold.",
  "nextStep": "Resume polling or manually check URL when ready",
  "question": "Resume polling now, or check back later?",
  "risk": "If Kopi is genuinely hung (not merely slow), manual intervention on their platform may be needed to unstick it."
}
```
